CARPETA ".vscode"

En esta carpeta se encuentran los ajustes necesarios para poder compilar, depurar y ejecutar un programa mediante el uso de la opción integrada de VSCode.

Para instalar el debugger (depurador) de C, deben ejecutar el siguiente comando en la consola de Ubuntu:

sudo apt install gdb